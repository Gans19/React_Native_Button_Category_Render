import React from 'react';
import { SafeAreaView } from 'react-native';
import DataDisplay from './DataDisplay';

const App = () => {
  return (
    <SafeAreaView>
      <DataDisplay />
    </SafeAreaView>
  );
};

export default App;
